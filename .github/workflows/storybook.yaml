name: Deploy Storybook to GitHub Pages
on:
  push:
    branches:
      - master
permissions:
  contents: read
  pages: write
  id-token: write
jobs:
  build-and-deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4.2.2
      - name: Build Storybook
        uses: ./.github/actions/storybook-build
      - name: Deploy Storybook
        uses: ./.github/actions/storybook-deploy
